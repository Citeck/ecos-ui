import React from 'react';
import Loader from '../../../components/common/Loader/Loader';

export default () => {
  return <Loader blur rounded style={{ position: 'relative', margin: '2em 0' }} />;
};
