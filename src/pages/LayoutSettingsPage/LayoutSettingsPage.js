import React from 'react';
import { connect } from 'react-redux';

const mapStateToProps = state => ({});

const mapDispatchToProps = dispatch => ({});

class LayoutSettingsPage extends React.Component {
  render() {
    return <div>тест</div>;
  }
}

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(LayoutSettingsPage);
