# Доклиб: UI-замечания по результатам проверки (COREDEV-355, пп. 1, 6, 7, 9, 10, 11, 12 + разбор п. 8)

Замечания QA к переписанной библиотеке документов (комментарий Ирины Прудниковой от 31.08.2026).
Правились те, что однозначно баги с понятным исправлением; остальные (2, 3, 4, 5) — отдельной
задачей. Ветка `bugfix/COREDEV-355` от `hotfix/2.29.2`.

Все замеры — computed styles на локальном стенде (`news-journal`, `viewMode=document-library`),
тест — `DocLib/__tests__/reviewRemarks.test.js` (каскад по скомпилированным SCSS + рендер свёрнутой
панели). Без правок падают 11 из 13 проверок.

## 1. Мелкий шрифт в дропдауне «Создать»

**Корень.** Общий `Dropdown` всегда вешает на меню класс `ecos-dropdown__menu_new`, а в
`Dropdown.scss` для `.ecos-dropdown__menu_new li` задано `$font-size-extra-small` (7.5pt = 10px).
Это системное правило: меню «Создать» в журнале даёт те же 10px/600. В 2026.2 доклиб пользовался
тем же `Dropdown`, так что это не регрессия, а общий вид всех дропдаунов — ровно как и написала Ира
(«общая проблема системы»).

**Правка — только для доклиба.** Менять глобальное правило в хотфиксе не стал (затронет каждое меню
в системе). Дропдауну «Создать» передан `menuClassName="citeck-doclib-toolbar__create-menu"`, и в
`DocLibToolbar.scss` пункт этого меню получает `$font-size-standard` — размер, на котором живёт вся
библиотека (дерево, список, плитки, сама кнопка). Прецедент того же приёма:
`DashboardSettings/style.scss` (`&.ecos-dropdown__menu_new li { font-size: $font-size-standard }`).
После: 13.33px, min-height 28px не тронут.

Если решат поднимать размер во всех дропдаунах — это одно правило в `Dropdown.scss`, и доклибный
override тогда можно убрать.

## 6. Левая плитка без отступа от рамки

**Корень.** `.citeck-doclib-files_grid { padding: 8px 0 }` — по горизонтали нулевой отступ, а у
области файлов `padding: 4px 0 8px`. Первая плитка стояла на x = рамка + 1px.

**Правка.** `padding: 8px 12px` — тот же горизонтальный отступ, что у строки списка и шапки списка
(`0 12px`). После: плитка на 13px от рамки (1px рамки + 12px).

## 7. Действия на плитке: без левого отступа, на белом фоне

**Корень.** Панель действий была `position: absolute; top: 6px; right: 6px` с авто-шириной и белой
подложкой `rgba(white, 0.9)`. Ширина одного действия — 26px (16px глифа + `ecos-btn_x-step_10`
даёт `margin-right: 10px`), плитка `minmax(168px, 1fr)`. Уже с 7 действиями (у docx на стенде их
8) панель шире плитки: выезжает за левый край, режется `overflow: hidden` карточки и ложится белым
прямоугольником поверх голубого превью и верха иконки файла. У папки без действий на ховере
оставалась белая точка 4×4 (padding пустой панели, `:empty` не гардился).

**Правка.** Панель перенесена внутрь превью первым дочерним элементом и разложена в потоке:
превью — колонка, панель — полоса `min-height: 28px; padding: 0 6px; justify-content: flex-end;
gap: 2px; flex-wrap: wrap`, без собственного фона, `margin-right` у кнопок обнулён; иконка файла
центрируется в остатке (`margin: auto`). Минимальная ширина плитки поднята до 180px — восемь
действий по 18px + 7 зазоров + 12px отступа = 170px помещаются в одну строку; более длинный набор
переносится и растит превью, а не вылезает за плитку. Превью 96px → `min-height: 112px`, чтобы
52px-иконка не упёрлась в полосу. Пустая панель — просто пустая полоса того же цвета, точки нет.

После (8 действий на 221px плитке): панель 1250–1470 внутри карточки 1249–1471, кнопки
1305–1464, фон прозрачный, иконка ниже полосы (y 270, полоса 226–254).

## 9. Свёрнутое дерево: вертикальный заголовок, мелкий шрифт, голубой ховер

**Корень.** В свёрнутом состоянии `FolderTreePanel` рендерил `citeck-doclib-panel__collapsed-title`
с `writing-mode: vertical-rl; font-size: $font-size-small` (11.33px), а сама полоса подсвечивалась
`$color-blue-light3` на `:hover`.

**Правка.** Заголовок убран из разметки вместе с его стилем; `:hover` у `_collapsed` снят. Осталась
полоса 36px с шевроном: она по-прежнему кликабельна и подписана через `title` («Развернуть панель
папок»), а обратную связь на ховер даёт сама кнопка-шеврон. Изначально это была серая заливка
`$color-gray-4`; по повторной проверке (см. ниже, п. 9) заливка снята — шеврон отвечает только
цветом, как `__toggle` в дереве.

## 10. Подсветка ховера и выделения строк отличается от журнала

**Замеры до.** Строка списка: ховер — заливка `$color-blue-light4`, выделение —
`var(--light-primary-color, $color-blue-light2)` = `#c6d6f0`. Иконка папки —
`var(--light-primary-color)` — тот же цвет, поэтому на выделенной строке папка «сливалась» (это же
п. 4 замечаний и п. 3 описания задачи). Журнальная таблица (`Grid.scss`, `Journals/style.scss`):
ховер — `.ecos-grid__row_new:hover td` рисует линии сверху и снизу цветом `$color-brown-2`
(`box-shadow inset`), заливка не меняется; выделение — `.ecos-grid__tr_selected` заливка
`$color-yellow-1` (#fff6d9), подложка инлайн-действий — градиент в тот же жёлтый.

**Правка (`FilesArea.scss`).** Строка: `:hover` → `box-shadow: 0 1px 0 0 $color-brown-2 inset,
0 -1px 0 0 $color-brown-2 inset`, без заливки; `_selected` → `background: $color-yellow-1`,
подложка `__row-actions` в выделенной строке — градиент в `$color-yellow-1` (как
`.ecos-inline-tools_selected > &-actions`). Плитка — те же цвета, чтобы режимы не расходились:
`:hover` добавляет рамку `$color-brown-2` к тени, `_selected` — рамка `$color-brown-2` и заливка
`$color-yellow-1` (раньше синяя рамка + `--light-primary-color`, где иконка папки тоже терялась).
Иконка папки (`--light-primary-color`) на жёлтом читается. Дерево папок в этот заход не трогал; по
повторной проверке (см. ниже, п. 9) оно переведено на те же цвета.

## 8. Пагинация — разбор, без правки

Факты:

- Доклиб пользуется общим `Pagination` с `DEFAULT_DOCLIB_PAGINATION` (`packages/constants/src/docLib.ts`):
  `maxItems: 50`. Показывает «1-N из N» и стрелки, при `total = 0` не рендерится. Область файлов —
  скролл-контейнер, так что папка до 50 элементов скроллится, свыше — листается стрелками.
- Проп `hasPageSize` у `Pagination` **мёртвый**: он объявлен в propTypes, `getPageSize` есть, но в
  `render` селектор размера страницы не выводится. Сменить размер страницы нельзя ни в доклибе, ни
  в журнале — так было и в 2026.2 (`DocLibPagination` передавал тот же `hasPageSize`).
- Журнальная таблица считает `maxItems` от высоты окна (`JournalsDashletPagination`:
  `maxHeightJournalData` / `HEIGHT_GRID_ROW`, минимум 6) и потому не скроллится по вертикали —
  это и есть «динамическое отображение», о котором пишет Ира. У доклиба такой привязки нет и не
  было; для сетки плиток она и не определена однозначно (число рядов зависит от ширины).
- Тот же `Pagination` в журнале точно так же показывает «1-4 из 4» с неактивными стрелками при
  одной странице — это не расхождение.

Вывод: работает адекватно и так же, как в 2026.2; ломаться нечему. Единственное, что можно
обсуждать как продуктовое решение (п. Павла про объединение ряда с кнопками и крошками) —
прятать пагинатор при `total <= maxItems` или считать `maxItems` от высоты области, как в
таблице. Ни то, ни другое в этой задаче не делал.

## Проверено (первый заход)

- `yarn test:ci src/components/journals/Journals/DocLib` — 5 сьютов, 31 тест, зелёные.
- В браузере (1500×900): дропдаун 13.33px; ховер строки — прозрачный фон + линии
  `rgb(228, 217, 184)`; выделение `rgb(255, 246, 217)`, папка `#c6d6f0` видна; первая плитка на
  13px от рамки; 8 действий внутри плитки без фона; свёрнутая полоса без текста и без ховер-заливки,
  шеврон на ховере серый.
- `prettier --check` по изменённым файлам чист; `tsc --noEmit` — 18 ошибок, все вне DocLib
  (были до правки).

## Повторная проверка 2026-09-08 (пп. 1, 9, 10, 11, 12)

QA перепроверила библиотеку и оставила пять замечаний. Замеры «до» — computed styles на локальном
стенде; тесты — `DocLib/__tests__/reviewRemarks.test.js` (дополнен) и
`dashboard/Dashlet/__tests__/headerGeometry.test.js` (шапки доклиба добавлены к общему правилу).
Без правок падают 13 из 38 проверок.

### 1. Кнопка «Создать» — только иконка

**Корень.** Кнопка тулбара рисовалась с подписью на десктопе (`{!isMobile && t(CREATE)}`) и
асимметричным `padding: 0 14px 0 10px` — 91.6×34. Рядом стоят иконочные кнопки 34×34 (обновить,
режим отображения), и «Создать» выбивалась из ряда шириной и текстом.

**Правка.** `DocLibToolbar.tsx`: подпись убрана и на десктопе, остался глиф `icon-small-plus`;
кнопка подписана `title` и `aria-label` (`btn.create.label`), так что скринридер и подсказка её
по-прежнему называют. `DocLibToolbar.scss`: `padding: 0; width: 34px; justify-content: center` —
квадрат той же высоты 34px и того же синего цвета; правило `&__create-btn_mobile` (оно только
подменяло padding) удалено как ставшее пустым. Обе ветки — одиночный вариант и дропдаун —
пользуются одним `createBtn`, поэтому иконочными стали обе.

CTA пустой папки (`Files/EmptyState.tsx`, `.citeck-doclib-empty__create-btn`) оставлена с подписью:
это призыв к действию посреди пустой области, а не элемент плотного ряда тулбара.

**После (замеры):** кнопка 34×34, `padding: 0`, `title`/`aria-label` = «Создать», `.ecos-btn__text` в разметке нет; соседние иконочные кнопки тулбара — те же 34px.

### 9. Подсветка дерева папок — как в журнале, без серого квадрата на шевроне

**Корень.** Строки дерева остались на старой схеме: ховер — серая заливка `$color-gray-4`,
выделение — `var(--light-primary-color)` с синим текстом и синей иконкой папки. Список файлов ещё в
прошлый заход переехал на журнальные цвета (п. 10), и одна и та же папка подсвечивалась в дереве
синим, а в списке жёлтым. Кнопка-шеврон в шапке панели на ховере получала заливку `$color-gray-4` —
серый квадрат 24×24, единственная заливка, оставшаяся в панели.

**Правка (`FolderTreePanel.scss`).** Строка: `:hover` → те же две внутренние линии
`$color-brown-2`, что у строки списка, без заливки; `_selected` → `background: $color-yellow-1`,
цвет текста и иконки папки не переопределяются (иконка остаётся `--light-primary-color` и на жёлтом
читается — это уже проверено на строках списка, §10). Выделение переживает ховер само собой:
`:hover` больше не объявляет `background`, так что жёлтый не перекрывается, а линии ложатся сверху.
Шеврон: заливка на `:hover` снята, остался только переход цвета к `$text-color-dark` — ровно как у
`&__toggle:hover` в дереве; `transition` переписан с `background-color` на `color`.

Чтобы дерево и список больше не разъезжались, объявление линий вынесено в
`DocLib/_mixins.scss` (`@mixin doclib-journal-row-hover-lines`) и подключается обоими файлами.
Свёрнутая полоса (`&_collapsed`) по-прежнему без ховер-заливки — это гардит `reviewRemarks.test.js`.

**После (замеры):** выделенная папка в дереве — `background rgb(255, 246, 217)` (`$color-yellow-1`), текст `rgb(68, 68, 68)`, иконка папки `rgb(183, 183, 183)`; ховер строки — фон прозрачный, `box-shadow rgb(228, 217, 184) 0 1px inset / 0 -1px inset` — байт в байт как у строки списка; шеврон в шапке на ховере — фон `rgba(0, 0, 0, 0)`, цвет `rgb(68, 68, 68)`.

### 10. Блок действий строки — внутри строки

**Корень.** `.citeck-doclib-files__row-actions` — `top: 0; height: 100%`, то есть ровно 44px строки,
с непрозрачной подложкой-градиентом (белым, а на выделенной строке жёлтым). Линии ховера рисуются
`box-shadow … inset` по краям строки, и подложка ложилась поверх них: у правого края строки линии
пропадали.

**Правка (`FilesArea.scss`).** `top: 1px; height: calc(100% - 2px)` — подложка отступает ровно на
толщину линий. `right: 0`, оба градиента и логика появления не изменились. Тот же приём в журнале:
`InlineTools.scss` — `margin-top: 2px; height: calc(40px - 2px)`, `Journals/style.scss` —
`height: calc(100% - 2px); margin-top: 1px`. Плитки не затронуты: их полоса действий
(`&__card-actions`) лежит в потоке внутри превью и ни `top`, ни `height` не имеет (общий у них
только миксин кнопок `doclib-inline-actions-btn`).

**После (замеры):** строка 44px (y 385), блок действий 42px (y 386), правый край общий (1583); на выделенной строке под ховером — фон `rgb(255, 246, 217)`, подложка `linear-gradient(… rgb(255, 246, 217) 15%)`, линии ховера на месте. Плитка 195×168: полоса действий `position: static`, 28px, внутри превью.

### 11. Шрифт заголовков колонок списка

**Корень.** `.citeck-doclib-files__head` — `font-size: 11px` (захардкожено, с комментарием «как в
`HeaderFormatter`»). Но `.ecos-th` в журнале живёт на `$font-size-standard` (13.33px) —
11px там нет; в итоге шапка списка была единственным местом библиотеки мельче 13.33px (см.
`doclib-typography-tree-base.md`).

**Правка.** `font-size: $font-size-standard`, вес остался `$font-weight-bold` — теперь ровно как
`.ecos-th`. Тест сравнивает не число, а скомпилированное значение `.ecos-th`, так что смена
константы не разведёт их снова.

**После (замеры):** `.citeck-doclib-files__head` — 13.33px / 700 (было 11px), имя файла в строке — 13.33px.

### 12. Высота собственных шапок — как у виджета (COREDEV-482)

**Корень.** Обе шапки библиотеки задавались содержимым, а не общей константой:
`.citeck-doclib-panel__header` — `padding: 12px 12px 10px 16px` + кнопка 24px + рамка = **47px**,
`.citeck-doclib-files__head` — `height: 40px`. Рядом на той же странице шапка виджета — 36px.

**Правка.** Обе переведены на `$dashlet-header-height` (36px) с `box-sizing: border-box`, то есть
нижняя граница 1px входит в высоту — правило из `docs/widget-header-geometry.md`.

- Шапка дерева: `padding: 0 12px 0 16px`. Левый отступ прежний: текст заголовка в 17px от внешнего
  края панели (1px рамки + 16px), ровно там же, где начинается содержимое тела виджета. Правый
  отступ оставлен библиотечным 12px, а не виджетным `$dashlet-header-padding-right` (8.33px):
  8.33px — это калибровка конкретной кнопки действия дашлета (глиф 15.33px, 1px внутреннего отступа
  кнопки) под биссектрису углового скругления `$border-radius-extra-large`; здесь другой блок
  (шеврон 24×24 с svg 14px) и другой радиус панели (`$border-radius-large`), так что то же число
  дало бы другую оптику, а не ту же. 12px — отступ, на котором стоят строка списка, шапка списка и
  сетка плиток, и он не двигает кнопку с уже проверенного места. Заголовку добавлен
  `line-height: inherit` (как у `.dashlet__caption`), чтобы строчный бокс не тянул высоту.
- Шапка списка: `height: $dashlet-header-height; box-sizing: border-box`, `padding: 0 12px`
  сохранён — текст колонок остаётся на одной вертикали с содержимым строк.

`.citeck-doclib__context-bar` (34px, крошки и пагинация) не тронут — это не шапка, и его высоту
гардит `contextBarHeight.test.js` (см. `doclib-context-bar-height-jump.md`).

Обе шапки добавлены в `dashboard/Dashlet/__tests__/headerGeometry.test.js` рядом с виджетом дерева:
высота сверяется с `.dashlet__header-wrapper`, `box-sizing` — `border-box`, левый отступ шапки
дерева — с суммой отступов общей шапки и её caption.

**После (замеры):** шапка дерева 36px (`border-box`, `padding 0 12px 0 16px`), текст заголовка на 17px от внешнего края панели (276 − 259), `line-height 20px`, шеврон 24px по центру (y 136.5 при верхе панели 130); шапка списка 36px (`border-box`), текст колонок на 12px от рамки; контекст-бар по-прежнему 34px; свёрнутая полоса 36px.

### Проверено

- `CI=true yarn test --watchAll=false src/components/journals/Journals/DocLib` и
  `src/components/dashboard/Dashlet/__tests__` — 8 сьютов, 69 тестов, зелёные. До правок:
  13 падений (4 в `headerGeometry`, 9 в `reviewRemarks`).
- `prettier --check` по изменённым файлам чист; `tsc --noEmit` — те же 18 ошибок, все вне DocLib.
- В браузере (1600×950, `news-journal`, `viewMode=document-library`): все замеры выше сняты после правки; режим плиток, свёрнутая полоса и контекст-бар без изменений.

### Попутно: шеврон свёрнутой полосы

Свёрнутая полоса держала шеврон на `padding-top: 10px`, а в развёрнутой шапке он теперь центрирован
в 36px (5.5px сверху) — при сворачивании шеврон прыгал бы на 4px (до правки — на 2px). Отступ полосы
выведен из той же геометрии: `($dashlet-header-height - 1px - 24px) / 2`, шеврон стоит на одном
месте в обоих состояниях.
