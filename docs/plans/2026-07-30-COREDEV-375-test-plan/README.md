# Test Plan: чат AI-ассистента — гашение устаревших кнопок и три дефекта интерфейса (COREDEV-375)

**Дата:** 2026-07-30
**Автор:** Антон Иванов (Claude Code)
**Ветка:** `ai-assistant-chat-ui-defects` @ `dac70544f` (23 коммита над `develop`)
**Стенд:** `http://localhost` (profile `local`, классификация `local`, auth BASIC `admin:admin`)
**Точка входа UI:** `http://localhost:3000` — vite-express dev-сервер ecos-ui, проксирует на стенд
**Тестовый workspace:** `user$admin`  •  **Run-id:** `run1`

## TL;DR

Приёмка правок фронтенда по COREDEV-375. Четыре исходных дефекта (FE-1…FE-4) плюс 12 дефектов,
найденных ревью поверх них. Реализация и ревью выполнены прогоном ralphex (5 ч 27 мин).

Особенность прогона: **приёмка ревьюера устарела**. Случаи A1–A10 гонялись на коммите `a05b07f5c`,
после него легло 12 коммитов, меняющих поведение. Поэтому здесь всё перепрогоняется на HEAD, плюс
закрываются пробелы, которые ревьюер оставил непроверенными.

**Текущий статус:** см. `reports/2026-07-30-run1.md`.

## ⚠ Правило окружения и safety

- Стенд `http://localhost`, `destructive_allowed: true` (см. `docs/plans/.test-stands.yml`).
- `test_connection` → `url == http://localhost`, profile `local` — проверено на safety-гейте.
- `ept_profile`/`docs_profile` остаются на production — это read-only (трекер, документация).
- Мутации ограничены workspace `user$admin` и данными, созданными этим прогоном.
- ⚠ **Ловушка zsh:** неэкранированная переменная не разбивается на слова, поэтому
  `AUTH='-u admin:admin'; curl $AUTH …` даёт **ложный 401**. Использовать явный заголовок:
  `-H "Authorization: Basic YWRtaW46YWRtaW4="`.
- ⚠ **citeck-ai держит параллельная сессия** (`mvnw spring-boot:run`, вне docker). Он
  перезапускался в 17:46 и 20:36. Перезапуск посреди запроса убивает его в полёте
  (`NoClassDefFoundError` в потоке `universal-request-…`) и выглядит как зависание интерфейса.
  При необъяснимом зависании — сверить время с логом citeck-ai, прежде чем заводить дефект.

## Карта документов

| Файл | Что содержит |
|---|---|
| `cases/preflight-smoke.md` | T1–T6, S1–S3 |
| `cases/regression.md` | R1–R4 — области, задетые правками ревью, но не связанные с FE-1…FE-4 |
| `cases/gate-liveness.md` | G1–G8 — FE-1, главный раздел |
| `cases/hint-and-ui-details.md` | H1–H4 (FE-2), P1–P3 (FE-3), Z1–Z2 (FE-4) |
| `cases/cross-cutting.md` | C1–C4 |
| `reports/2026-07-30-run1.md` | Результат прогона |

Исходный план реализации: `docs/plans/completed/20260730-ai-assistant-chat-ui-defects.md`.

## Tier-модель

| Tier | Кейсы | Метод | Параллелизация |
|---|---|---|---|
| **A** | T1–T5, G8, часть C | scripted curl + Records API + лог citeck-ai | Свободная |
| **B** | S, R, G1–G7, H, P, Z | Playwright (один браузер) | Последовательно |

Перекос в сторону Tier B здесь закономерен: все четыре дефекта — про рендер и жизненный цикл
элементов интерфейса, через REST их не увидеть.

## Кластеры конфигов

| # | Конфиг | Кейсы | Что меняется |
|---|---|---|---|
| 0 | Pre-flight + unit | T1–T6 | — |
| 1 | Default | всё остальное | дефолт |

Конфиг-переключателей у этой фичи нет, рестарты сервисов не требуются — **return-to-defaults
сводится к проверке, что `git status` чист** (правки на стенде: только `aiModel` двум агентам,
см. ниже).

## Изменения на стенде, сделанные для прогона

| Запись | Поле | Было | Стало | Откат |
|---|---|---|---|---|
| `emodel/ai-agent@c1b845b7-…` («Тест: файловые инструменты») | `aiModel` | `gpt-5.4` | `gpt://b1g9il6bnhcqq6bnvrtl/deepseek-v4-flash/latest` | вернуть `gpt-5.4` |
| `emodel/ai-agent@image-helper` | `aiModel` | `gpt-5.4` | то же | то же |

Причина: `gpt-5.4` — имя в стиле OpenAI, шлюз ожидает URI формата Yandex и отвечает
`HTTP 400 Failed to parse model URI`, из-за чего оба агента отдавали 500 на любой запрос и случай
G5 (A7) был заблокирован.

## Известно недостижимое (SKIP с обоснованием, не гонять)

| Что | Почему |
|---|---|
| Переключатели области развёртывания | `citeck-ai/.../config/PendingDeploy.kt:68-88` — `resolveDeployScope` в **обеих** ветках возвращает `changeable = false` и `options` из одного элемента. `changeable = true` не встречается в `src/main` нигде. Условие `showSelector` (`DeployConfirmation.jsx:39`) недостижимо при текущем бэкенде. Решение COREDEV-323 Task 6. |
| Повторяемая ошибка сохранения файла | Состояние бэкенда, которое стенд не выдаёт по требованию. |
| Гейт эскалации после ответа свободным текстом | Ухудшение относительно `develop`, но правка принадлежит citeck-ai. Проверять на фронте нечего. **Исправлено 2026-07-31** коммитом citeck-ai `5a9bace` — см. B1 в разделе «⚠ ЧТО ОСТАЛОСЬ». |

## ⚠ ЧТО ОСТАЛОСЬ (состояние на 2026-08-04; исходный срез — 2026-07-31, после прогона run1)

Приёмка run1 пройдена: **29 из 34 случаев, ни одного FAIL**, все четыре заявленных дефекта
FE-1…FE-4 подтверждены исправленными на живом интерфейсе. Ниже — всё незакрытое, сгруппированное
по тому, кто и когда должен это сделать.

### 1. Блокировало выпуск ветки — закрыто

| # | Что | Детали |
|---|---|---|
| B1 | ~~Ухудшение с гейтом эскалации~~ **ИСПРАВЛЕНО 2026-07-31** | Было: после ответа свободным текстом на гейт эскалации старая карточка гаснет по позиционному правилу, а новая `CONFIRM` не предлагает. На фронте не чинилось (`CONFIRM` эскалации неотличим от `CONFIRM` гейта плана, идентификаторы переиспользуются намеренно). Правка сделана в citeck-ai коммитом `5a9bace` «fix(escalation): keep the remediation action on a free-text answer»: ветка `AgentAction.QUESTION` теперь развилкой по `state.pendingEscalation` отдаёт `ESCALATION_ACTIONS` + `buildEscalationMessage` вместо безусловных `STEP_ERROR_ACTIONS` (`src/main/java/ru/citeck/ecos/ai/domain/assistant/core/AgentOrchestratorService.kt:5294-5325`; прежняя ссылка `:4728` устарела). Добавлены два теста в `AgentOrchestratorServiceTest` и случай I23 регрессионного набора. **Уточнение к исходной формулировке:** тупика не было и до правки — пропуск и отмена оставались, а общий идентификатор `CONFIRM` и раньше уходил в восстановительную ветку; терялась именно кнопка «Добавить поля и продолжить», причём «Повторить» вместо неё бесполезен (перезапускает шаг в те же отсутствующие поля). ⚠ Фикс живёт только в ветке `feature/COREDEV-323-operational-config-agent-split` — см. B2 |
| B2 | **Порядок выпуска относительно citeck-ai — стал актуальным** | Фронт COREDEV-375 влит в `develop` ecos-ui (см. B3), а обе зависимости на стороне citeck-ai — `attachPendingFilesSnapshot` (`bfc7162`) и фикс гейта эскалации (`5a9bace`) — лежат только в ветке `feature/COREDEV-323-operational-config-agent-split`; в `develop` и `master` citeck-ai их нет (проверено 2026-08-04). Пока эта ветка не влита: гашение файловых кнопок бездействует (не ломается), а дефект B1 на стенде с develop-сборкой citeck-ai воспроизводится |
| B3 | ~~Push и MR~~ **ВЛИТО 2026-08-04** | Ветка `ai-assistant-chat-ui-defects` влита в `develop` ecos-ui squash-мерджем `bba9e8ae4` («Четыре дефекта интерфейса чата AI-ассистента») и merge-коммитом `35acbbdc3`; удалённая и локальная ветки удалены |

### 2. Следующая работа в ecos-ui

| # | Что | Детали |
|---|---|---|
| N1 | **FE-7 — снять условие `hasLiveSnapshot`** | Задача заведена в `citeck-ai/docs/plans/20260731-ecos-ui-tasks.md`. Сервер теперь всегда шлёт `pendingFiles`, включая `[]`; `null` остаётся только у конверта ошибки контроллера. В `useUniversalChat.js` (~строка 470) достаточно `Array.isArray(result.pendingFiles)`. **Две оговорки, зафиксированные при разборе:** (а) `pendingFileActionTempRef` используется ещё и для вычисления `deadTempFileId` под `stripTempImageFromText` (COREDEV-313/321) — гашение кнопок переводить на массив можно, а вырезание превью по одному массиву опасно: в тексте бывают превью загруженных изображений, которые предложениями не были; (б) порядок выпуска безопасен — со старым бэкендом `Array.isArray(null)` даёт `false`, механизм просто бездействует |
| N2 | **Перепрогнать G5 после FE-7** | Сегодняшняя проверка сделана на текущем коде; FE-7 меняет механику гашения файловых кнопок |
| N3 | **Решение по placeholder (P3)** | При невыбранном агенте поле ввода показывает конфигурационную формулировку, а значок записи по умолчанию — «Operational». Подтверждено на стенде. Вторая такая же ветка: редактирование скрипта перенаправляется на конфигурационного агента, placeholder остаётся операционным. Решение плана (случай 22) — оставить; ревью подтвердило четырежды. **Требуется явное решение владельца** |
| N4 | **Мелочи, оставленные ревью ниже порога** | Пересчёт признака устаревания O(n²) на каждый такт опроса; избыточный операнд `actionsResolved` в `AgentPlanMessage:57`; мёртвый реэкспорт `fileSaveActionTempRef`; недостижимое запасное сопоставление по `action.id` в `handleActionClick`; правки `ScriptDiffViewer` и `ChatContextTags` не отражены в списке файлов плана реализации |

### 3. Незакрытые случаи приёмки (5 из 34)

Ни один не относится к сути COREDEV-375, все покрыты модульными тестами.

| Случай | Причина | Как закрыть |
|---|---|---|
| H3 | Живой гейт без кнопок — состояние не воспроизведено | Нужен ответ бэкенда с `WAITING_PLAN_APPROVAL` без `actions` |
| H4 | Терминальная карточка `FAILED` не воспроизведена | Нужен обрыв шага выполнения |
| P2 | Контекстная вкладка требует контекста редактора BPMN (`hasContext`, `AIAssistantChat.jsx:500`), а не карточки записи | Открыть редактор модели BPMN и проверить placeholder `…contextual` |
| R1 | `ScriptDiffViewer` — нужна запись с полем-скриптом и предложение правки | Проверяет снятие нарушения порядка хуков (`useMemo` после раннего `return null`), Codex подтвердил реальность риска |
| R3 | Обычное markdown-сообщение с кнопками за прогон не встретилось | Например, подтверждение изменения записи операционным агентом |

### 4. Кандидаты в задачи по citeck-ai (не заводились)

| # | Что | Где |
|---|---|---|
| A1 | `GenerateDataTypeTool` возвращает `success=true` при **нуле** строк изменений; рядом дважды `DraftBasedLlmGenerator: No draft found after LLM call on attempt 1` | Наблюдалось 4 раза подряд 2026-07-30 |
| A2 | Пустое пользовательское сообщение доходит до Spring AI и роняет запрос: `IllegalArgumentException: text cannot be null or empty` в `DefaultChatClient$DefaultChatClientRequestSpec.user`. Фронт при отправке действия штатно шлёт `message: ''` — отсекать надо на входе | `citeck-ai`, 2026-07-30 20:38 |
| A3 | Ветки «действие не поддерживается» возвращают карточку без кнопок | `AgentOrchestratorService.kt:3202`, `:4742` |

### 5. Посторонние дефекты интерфейса (не связаны с COREDEV-375)

| # | Что | Статус |
|---|---|---|
| D1 | Смешанная локализация в одном ряду кнопок: `Skip` рядом с «Отмена», `Cancel` рядом с «Сохранить в «Сгенерировано ИИ»» | Найден в приёмке ревьюера, **жив** |
| D2 | Заголовок страницы — «сырой» ключ `#ECOS_PAGE_TITLE` | Найден в run1 |
| D3 | Фон консоли FE-5: `defaultProps`, `StrictMode`, `componentWillReceiveProps`, `findDOMNode`, `column.dataField`, `key` | Вынесен в отдельную задачу, в объём COREDEV-375 не входит |

### 6. Состояние стенда после прогона

- Правка `aiModel` двум агентам **в силе**; откат не рекомендуется — прежнее значение `gpt-5.4` давало 500 на любой запрос.
- Созданы: `fileA.txt`, `fileB.txt` (папка «Сгенерировано ИИ»), тип `emodel/type@user__admin:zayavka-pw2`, артефакты бизнес-приложения «Заявка на пропуск» — всё в ws `user$admin`.
- `git status` в ecos-ui чист, кроме файлов этого тест-плана и `docs/plans/.test-stands.yml`.
- ⚠ citeck-ai держит **параллельная сессия**; за прогон он перезапускался трижды и один раз выпал из шлюза на секунду (404 на запросе 2707). При необъяснимом зависании или 404 — сверять с логом citeck-ai, а не заводить дефект фронта.

## Порядок прогона

0. Safety-гейт ✔, декларация стендов ✔, перезапуск dev-сервера на HEAD ✔.
1. T1–T6 лично.
2. Кластер 1: S лично → Tier B по разделам (G → H → P → Z → R) → Tier A (G8) → C.
3. Анализ FAIL, дозаполнение отчёта, вердикт.

## Done criteria

- [ ] T пройдены (или нерабочие инструменты зафиксированы)
- [ ] Unit-suite зелёный
- [ ] R пройдены
- [ ] G/H/P/Z пройдены или явно SKIP с обоснованием
- [ ] C-кейсы без ошибок
- [ ] Return-to-defaults: `git status` чист, правки `aiModel` либо откачены, либо согласованы
