# Test Run Report: COREDEV-375 — чат AI-ассистента, гашение устаревших кнопок и три дефекта интерфейса

**Дата прогона:** 2026-07-30  •  **Run-id:** `run1`
**Тестировщик:** Антон Иванов (Claude Code)
**Окружение:** стенд `http://localhost` (profile `local`, классификация `local`), ветка
`ai-assistant-chat-ui-defects` @ `dac70544f`; UI — dev-сервер `http://localhost:3000`,
перезапущен на HEAD перед прогоном.
**Кластеры:** 0 (pre-flight+unit), 1 (default). Конфиг-переключателей у фичи нет.

## Сводка

| Раздел | Всего | PASSED | FAILED | SKIPPED / не прогнано |
|---|---|---|---|---|
| Pre-flight (T) | 6 | 6 | 0 | 0 |
| Smoke (S) | 3 | 3 | 0 | 0 |
| Регрессия (R) | 4 | 2 | 0 | 2 |
| FE-1 гейты (G) | 8 | **8** | 0 | 0 |
| FE-2 подсказка (H) | 4 | 2 | 0 | 2 |
| FE-3 placeholder (P) | 3 | 2 | 0 | 1 |
| FE-4 размер (Z) | 2 | 2 | 0 | 0 |
| Cross-cutting (C) | 4 | 4 | 0 | 0 |
| **Итого** | **34** | **29** | **0** | **5** |

**Ни одного FAIL.** Все четыре исходных дефекта (FE-1…FE-4) подтверждены исправленными на живом
интерфейсе. Раздел FE-1 закрыт полностью — включая G8, ветку которой не удавалось проверить ни
одному проходу ревью.

## Детали

### Pre-flight (T) — все пройдены
| ID | Status | Комментарий |
|---|---|---|
| T1 | PASSED | `active`/`records` → `local`; `test_connection.url == http://localhost` |
| T2 | PASSED | docker UP, 14 контейнеров `tdcuosa`, корень 302 |
| T3 | PASSED | `/gateway/ai/api/ai-agent/list` → 200, 8 агентов |
| T4 | PASSED | dev-сервер перезапущен на `dac70544f`; `:3000` и прокси → 200 |
| T5 | PASSED | `yarn test:ci` — **204 набора / 2653 теста / 32 снимка**, всё зелёное |
| T6 | PASSED | Playwright MCP работает |

### Smoke (S) — все пройдены
| ID | Status | Комментарий |
|---|---|---|
| S1 | PASSED | Чат открывается по Cmd+I |
| S2 | PASSED | Список из 9 агентов, выбор переключает тег и сбрасывает диалог |
| S3 | PASSED | Запросы отрабатывают (покрыто по ходу G1–G5) |

### FE-1 — жизненный цикл кнопок гейтов (G)
| ID | Status | Комментарий |
|---|---|---|
| G1 | **PASSED** | Ответ свободным текстом гасит кнопки прошлого раунда **немедленно**, до ответа сервера: `disabled`, `--stale`, `opacity: 0.45`, `cursor: not-allowed`. Кнопки нового раунда живые |
| G2 | **PASSED** | Клик по `Skip` гасит кнопки своего раунда; прошлый раунд остаётся неактивным; новая карточка плана — с живыми кнопками |
| G3 | **PASSED** | Гейт развёртывания подтверждён (1 POST → 202); через **8 секунд** кнопки `disabled`+`--stale`, через 45 с — то же. Затем 4 синтетических клика (2 кнопки × `.click()` и `dispatchEvent`) → **0 POST**. Тип `emodel/type@user__admin:zayavka-pw2` развёрнут и подтверждён через Records API. Переключателей области нет (`radios: 0`) — соответствует `changeable = false` на бэкенде |
| G4 | **PASSED** | Негатив: на каждом шаге G1, G2, G5, H1 кнопки последнего сообщения активны; правило не гасит лишнего |
| G5 | **PASSED** | Два ожидающих файла: обе пары живы одновременно (сообщение A вытеснено дважды); сохранение A гасит **только** пару A; B сохраняется **ровно одним POST**; обе записи созданы в Records API |
| G6 | **PASSED** | Отмена активного запроса: карточка `--cancelled`, кнопки гейта **остаются** неактивными — оживления нет |
| G7 | **PASSED** | Во время обработки кнопки гейта заморожены, кнопка `Cancel` карточки выполнения живая |
| G8 | **PASSED** (непреднамеренно, но достоверно) | При первой попытке G3 POST подтверждения получил **404** (запрос 2707; соседние 2656 и 2710–2711 — 202). Фронт повёл себя ровно по связывающему решению плана: добавил уведомление «An error occurred while processing the action. Please try again.» и **оставил кнопки кликабельными** для повтора — признак разрешения в ветке `catch` не выставляется. Контраст с успешной попыткой (202 → гашение за 8 с) доказателен. Первая проверка этой ветки на стенде |

**Главное подтверждение:** измеренная `opacity` неактивной кнопки = **0.45**. До правки ревью
селектор `--stale` перебивался правилом `:disabled`, и стиль был мёртвым кодом. Теперь применяется.

### FE-2 — подсказка гейта плана (H)
| ID | Status | Комментарий |
|---|---|---|
| H1 | **PASSED** | **Впервые подтверждено на стенде.** Карточка `WAITING_PLAN_APPROVAL` воспроизведена (ревьюер за весь прогон не смог). После `Reject`: обе кнопки `disabled`+`--stale`, подсказка **не появилась** ни сразу, ни после терминального сообщения «План отменён. Можете начать новый запрос.». `hints: []` в DOM, степпер очищен |
| H2 | **PASSED** | Подтверждение плана: кнопки `disabled`+`--stale` через 6 с, подсказка **не появилась** ни сразу, ни через 60 с при идущем выполнении (`hints: []`). Выполнение стартовало («Executing the plan, Step 1 of 6»). Обе ветки FE-2 проверены на стенде |
| H3 | не прогнан | Живой гейт без кнопок — состояние на стенде не воспроизведено |
| H4 | не прогнан | Терминальная карточка `FAILED` — не воспроизведена |

### FE-3 — placeholder (P)
| ID | Status | Комментарий |
|---|---|---|
| P1 | **PASSED** | Без агента → `…universal`; `TOOL_LOOP` → `…operational` («Ask about tasks, documents or records...»); `CONFIG` → `…universal`; возврат к «Universal assistant» → `…universal` |
| P2 | не прогнан | Контекстная вкладка появляется только на карточке записи; на главной странице вкладок нет. Покрыт модульным тестом 26 |
| P3 | **ПОДТВЕРЖДЕНО (наблюдение)** | Запись по умолчанию «Citeck AI» помечена значком **Operational**, placeholder при этом конфигурационный. Несогласованность реальна. Решение плана (случай 22) — оставить |

### FE-4 — изменение размера (Z) — все пройдены
| ID | Status | Комментарий |
|---|---|---|
| Z1 | **PASSED** | **Ноль** упоминаний `disableResize` за 3982 строки консоли; на внутреннем `div` только `class` и `style` |
| Z2 | **PASSED** | Развёрнутым: 1 маркер, перетаскивание 480×680 → 625×795. Свёрнутым: 0 маркеров, высота 50. При разворачивании маркер возвращается |

### Регрессия (R)
| ID | Status | Комментарий |
|---|---|---|
| R1 | не прогнан | `ScriptDiffViewer` — требует записи с полем-скриптом. Правка снимала нарушение порядка хуков (`useMemo` после раннего `return null`), подтверждена Codex как реальный риск |
| R2 | **PASSED** | Кавычки-ёлочки в подписи «Сохранить в «Сгенерировано ИИ»» рендерятся как кавычки, а не как `&quot;`. Экранирование из последнего раунда ревью результат не испортило |
| R3 | не прогнан | Обычное markdown-сообщение с кнопками (ветка `MessageItem`) — не встретилось в прогоне |
| R4 | **PASSED** | Очистка контекста: 0 сообщений, приветственный экран |

### Cross-cutting (C) — все пройдены
| ID | Status | Комментарий |
|---|---|---|
| C1 | PASSED | Ошибки консоли только из известного фона FE-5: `defaultProps`, `StrictMode`, `findDOMNode`, `column.dataField`, `key`. Плюс `chrome-extension://invalid` — артефакт Playwright. **Новых нет** |
| C2 | PASSED | **123 запроса** к `/gateway/ai/`: 97 × `202`, 26 × `200`. **Ноль 4xx/5xx** |
| C3 | PASSED | Осмотр экрана — см. дефекты ниже. Битых изображений нет, `&quot;`/`null`/`undefined`/«сырых» ссылок на временные файлы в чате нет |
| C4 | PASSED | `git status` чист, кроме файлов этого тест-плана |

## Найденные дефекты

| # | Описание | Repro | Severity | Отношение к COREDEV-375 |
|---|---|---|---|---|
| D1 | **Смешанная локализация в одном ряду кнопок**: `Skip` (англ.) рядом с «Отмена» (рус.); то же в паре `Cancel` / «Сохранить в «Сгенерировано ИИ»» | Любой раунд уточнений; любое предложение сохранить файл | Minor | Не связан с правками. Найден в прошлой приёмке — **жив** |
| D2 | **Заголовок страницы — «сырой» ключ** `#ECOS_PAGE_TITLE` вместо переведённого текста | Любая страница платформы | Minor | Не связан с правками |

## Изменения на стенде

| Запись | Поле | Было | Стало |
|---|---|---|---|
| `emodel/ai-agent@c1b845b7-…` («Тест: файловые инструменты») | `aiModel` | `gpt-5.4` | `gpt://b1g9il6bnhcqq6bnvrtl/deepseek-v4-flash/latest` |
| `emodel/ai-agent@image-helper` | `aiModel` | `gpt-5.4` | то же |

Прежнее значение было нерабочим: `gpt-5.4` — имя в стиле OpenAI, шлюз ожидает URI формата Yandex и
отвечает `HTTP 400 Failed to parse model URI`. Оба агента отдавали 500 на любой запрос, из-за чего
случай G5 был заблокирован у ревьюера. **Откат не рекомендуется** — вернёт неработающих агентов.

Созданные прогоном записи: `emodel/workspace-file@fedfaeb9-…` (fileA.txt),
`emodel/workspace-file@b166bf92-…` (fileB.txt) в папке «Сгенерировано ИИ» workspace `user$admin`.

## Известно недостижимое (не гонялось осознанно)

| Что | Почему |
|---|---|
| Переключатели области развёртывания | `citeck-ai/.../PendingDeploy.kt:68-88` — `resolveDeployScope` в обеих ветках возвращает `changeable = false` и один вариант; `changeable = true` не встречается в `src/main`. Условие `showSelector` недостижимо при текущем бэкенде (решение COREDEV-323 Task 6) |
| Повторяемая ошибка сохранения файла | Состояние бэкенда, которое стенд не выдаёт по требованию |
| Гейт эскалации после ответа свободным текстом | Ухудшение относительно `develop`, но правка принадлежит citeck-ai (`AgentOrchestratorService.kt:4728`) |

## Артефакты, созданные прогоном на стенде

| Что | Где |
|---|---|
| `fileA.txt`, `fileB.txt` | `emodel/workspace-file@fedfaeb9-…`, `@b166bf92-…`, папка «Сгенерировано ИИ», ws `user$admin` |
| Тип «Заявка PW2» | `emodel/type@user__admin:zayavka-pw2`, ws `user$admin` |
| Бизнес-приложение «Заявка на пропуск» | Выполнение запущено в H2 (6 шагов); артефакты в ws `user$admin` |

Всё в личном рабочем пространстве администратора, чужие данные не задеты.

## Финальный вердикт

- [x] T пройдены (6/6)
- [x] Unit-suite зелёный (204 набора / 2653 теста / 32 снимка)
- [ ] R пройдены — 2 из 4 (R1 `ScriptDiffViewer`, R3 не прогнаны)
- [x] **FE-1 закрыт полностью** — G1–G8, 8 из 8
- [x] **FE-2 закрыт** — H1 (отклонение) и H2 (подтверждение), обе ветки впервые на стенде
- [x] **FE-3 закрыт** — P1
- [x] **FE-4 закрыт** — Z1, Z2
- [x] C-кейсы без ошибок
- [x] Return-to-defaults: `git status` чист (только файлы этого тест-плана)

**Решение: ✅ по существу задачи COREDEV-375 приёмка пройдена.** Все четыре заявленных дефекта
подтверждены исправленными на живом интерфейсе, ни одного FAIL, регрессов не обнаружено.

**Не прогнано (5 случаев, каждый с причиной, ни один не относится к сути COREDEV-375):**

| Случай | Причина |
|---|---|
| H3 | Живой гейт без кнопок — состояние на стенде не воспроизведено; покрыт модульными тестами 18–21 |
| H4 | Терминальная карточка `FAILED` — не воспроизведена |
| P2 | Контекстная вкладка требует контекста редактора BPMN (`hasContext` в `AIAssistantChat.jsx:500`), а не карточки записи; покрыт модульным тестом 26 |
| R1 | `ScriptDiffViewer` — требует записи с полем-скриптом и предложения правки от ассистента |
| R3 | Обычное markdown-сообщение с кнопками — за прогон не встретилось |

**Перед выпуском учесть:** после реализации FE-7 (снятие условия `hasLiveSnapshot` вслед за новым
контрактом `pendingFiles`) случай G5 придётся перепрогнать — механика гашения файловых кнопок
изменится.
